import { createContext } from "react";

export const useModalContext = createContext({});
export const useNetInfoContext = createContext({});
export const useDeviceDiscoveryContext = createContext({});
